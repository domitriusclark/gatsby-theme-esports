import colors from "./colors";

const elementStyles = {
  root: {
    fontFamily: "body",
    lineHeight: "body",
    background: colors.background,
    color: colors.text,
    maxHeight: "100%",
    maxWidth: "100%",
    margin: 0,
    padding: 0
  }
};

export default elementStyles;
